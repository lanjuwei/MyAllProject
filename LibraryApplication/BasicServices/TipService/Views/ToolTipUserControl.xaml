<UserControl x:Class="BasicServices.TipService.Views.ToolTipUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid >
            <Border   Padding="10" CornerRadius="15" Background="#B2000000"  HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock x:Name="Tb"  Text="{Binding}" Foreground="White" HorizontalAlignment="Center" TextWrapping="Wrap"  VerticalAlignment="Center" FontSize="39" Background="{x:Null}"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
